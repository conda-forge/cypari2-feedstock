{% set version = "1.2.1" %}
{% set name = "cypari2" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/defeo/cypari2/archive/{{ version }}.tar.gz
  sha256: 85697e61047c9b1e9132c846090d660b12511a1456a5add3c9f5dfb7568786fe

build:
  number: 1
  skip: true  # [win]
  script:
    - python -m pip install --no-deps --ignore-installed --verbose .

requirements:
  build:
    - {{ pin_compatible('pari', max_pin='x.x.x') }}
    - {{ compiler('c') }}
  host:
    - {{ compiler('c') }}
  host:
    - setuptools
    - cython
    - python
    - pari >=2.9
    - pip
    - cysignals
    - gmp
  run:
    - python
    - cysignals
    - gmp

test:
  imports:
    - cypari2
  commands:
    - python -c "import doctest; import cypari2; doctest.testmod(cypari2, optionflags=doctest.ELLIPSIS|doctest.REPORT_NDIFF)"
    - test -f $SP_DIR/cypari2/auto_paridecl.pxd

about:
  home: https://github.com/defeo/cypari2
  license: GPLv2+
  license_file: LICENSE
  summary: 'An interface to the number theory library libpari'

extra:
  recipe-maintainers:
    - isuruf
    - saraedum
