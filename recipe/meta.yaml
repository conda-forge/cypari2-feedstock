{% set version = "2.1.0" %}
{% set name = "cypari2" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/defeo/cypari2/archive/{{ version }}.tar.gz
  sha256: 11b5ff20524f665cc9723e19c27559da32b39da1c16c468ce17cd3d0341f4eda

build:
  number: 0
  skip: true  # [win]
  script:
    - python -m pip install --no-deps --ignore-installed --verbose .

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - setuptools
    - cython
    - python
    - pari >=2.9
    - pip
    - cysignals
    - gmp
  run:
    - python
    - {{ pin_compatible('pari', max_pin='x.x.x') }}
    - cysignals
    - gmp

test:
  imports:
    - cypari2
  commands:
    - python -c "import doctest; import cypari2; doctest.testmod(cypari2, optionflags=doctest.ELLIPSIS|doctest.REPORT_NDIFF)"
    - test -f $SP_DIR/cypari2/auto_paridecl.pxd

about:
  home: https://github.com/defeo/cypari2
  license: GPLv2+
  license_file: LICENSE
  summary: 'An interface to the number theory library libpari'

extra:
  recipe-maintainers:
    - isuruf
    - saraedum
